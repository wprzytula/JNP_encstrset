namespace {
    unordered_map sets;
    unsigned long lastID;
    unsigned long obtainID();
    const char* encrypt();
    void printEncrypted(const char* encrypted); // Prints <cypher "HEX HEX...">.
    void printSet(unsigned long id); // Prints <set #ID>.
     
    
}
